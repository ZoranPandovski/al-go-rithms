/* Author: Codiyapa
   two sum
*/

#include<bits/stdc++.h>
using namespace std;

int main(){
    unordered_map<int, int>m;
    int n;
    cin>n;
    int x;
    for(int i=0;i<n;i++){
        cin>>x;
        m[x]=1;
    }
    int sum, flag=0;
    cin>>sum;
    unordered_map<int, int>::iterator itr = m.begin();
    while(itr!=m.end()){
        if(m[itr->first] && m[sum-itr->first]){
            cout<<"yes\n";
            flag=1;
            break;
        }
    }
    if(flag==0)
        cout<<"No";
    return 0;
}

